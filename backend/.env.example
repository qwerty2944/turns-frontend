PORT=2567
JWT_SECRET=change-me-in-production
# Neon / Supabase / local Postgres connection string
DATABASE_URL=postgres://postgres:postgres@localhost:5432/turns
NODE_ENV=development
# For production: use a Neon URL with ?sslmode=require
